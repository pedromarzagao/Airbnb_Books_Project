<div class="container">
  <div class="row"></div>
    <div class="bookings-box">
      <% @bookings.each do |booking|  %>
          <%= booking.book.title %>
          <%= booking.book.address %>
          <%= booking.book.date_begin %>
          <%= booking.book.date_end %>
      <% end %>
    </div>
</div>
